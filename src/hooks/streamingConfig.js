export const STREAMING_CONFIG = {
  TYPING_SPEED: 2, // milliseconds per character (lower = faster)
  ROW_INTERVAL: 200, // milliseconds between rows appearing
  TYPING_DELAY: {
    PRIMARY: 0,   // delay before primary field starts typing
    SECONDARY: 200, // delay before secondary field starts typing
  }
};